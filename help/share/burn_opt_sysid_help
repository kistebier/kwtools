# burn_opt_sysid_help {{{
#
TITLE="${gv_Configuration}::${lv_Burn_Opt}::${gv_Help}"
MSG=$(gettext 'Hier können Sie einstellen welche Optionen für das
Erstellen eines ISO9660 Dateisystem benutzt, oder an das
Brennprogramm übergeben, werden sollen.

NOTIZ: Wenn die Option \"symbolischen Verknuepfungen folgen\"
\tausgewählt wurde kommt noch die Option \"-f\" hinzu.

LINUX/UNIX: Optionen sind zur Zeit folgende:
\t-U -R -gui -graft-points -max-iso9660-filenames -cache-inodes -iso-level 4

LINUX/UNIX+WINDOWS: Optionen sind zur Zeit folgende:
\t-U -R -J -joliet-long -gui -graft-points -max-iso9660-filenames -cache-inodes -iso-level 4

DOS: Optionen sind zur Zeit folgende:
-ldots -input-charset cp437 -no-cache-inodes -iso-level 2

UDF: Optionen sind zur Zeit folgende:
\t-U -R -J -joliet-long -gui -graft-points -udf -max-iso9660-filenames -cache-inodes -iso-level 4
')
#}}}
### Modeline {{{
### vim:ft=zsh:foldmethod=marker
### vim:set ts=4:                                                                               
### }}}

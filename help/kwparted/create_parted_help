# create_parted_help fuer kwparted{{{
TITLE="${PARTED_TITLE}::${gv_Help}"
MSG=$(gettext 'Groesse: Groesse der Partition eingeben. Als Masseinheit werden
\tfolgende Zeichen unterstuetzt: k, K, Kb, KB, m, M, Mb,...z, Z, Zb,
\tZB.

Dateisystem: Auswahl des Filesystem Typs. Zur Auswahl stehen
\text2/3/4, fat16, fat32, hfs(+), jfs, linux-swap (Swap-Partition),
\tntfs, reiserfs, reiser4, ufs, xfs.

Flags: Partitions Flag auswaehlen, wie lvm, raid oder boot.

Name: Vergeben Sie einen Namen fuer die Partition, ohne Leerzeichen.

NOTIZ: nur beim Partitionstabellentyp "gpt".

Partitions_Typ: Partitionstype auswaehlen (primaere, erweiterte
\toder logische Partition). Nach einer "erweiterten Partition"
\tkann man keine weitere "erweiterte Partition" anlegen.
\tPatitionstabelle hat folgendes Format:
\t- 4 mal primaere Partitionen und nichts weiter
\t- <= 3 primaere Partitionen und 1 erweiterte Partition.
\t- in der erweiterten Partition koennen Sie weitere logische
\tPartitionen anlegen.

NOTIZ: nur beim Partitionstabellentyp "msdos".
')
#}}}
### Modeline {{{
### vim:ft=zsh:foldmethod=marker
### vim:set ts=4:                                                                               
### }}}

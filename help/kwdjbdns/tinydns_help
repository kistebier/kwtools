# tinydns_help fuer kwdjbdns{{{
TITLE="${gv_Configuration}::${lv_Tinydns}::${gv_Help}"
MSG=$(gettext 'Ansicht: Zeigt alle Eintraege in
\t${SV_DIR}/tinydns/root/data.

Nameserver addieren: Addiert zur bestehenden Konfiguration einen 
\tNameserver hinzu. Diese werden alphabetisch angelegt
\ta,b,c,d...

Child Nameserver addieren: Addiert zur bestehenden Konfiguration einen 
\tChild Nameserver hinzu. Diese werden alphabetisch angelegt
\ta,b,c,d...

Rechner addieren: Addiert zur bestehenden Konfiguration einen 
\tIPv4 Rechner und IPv4 Alias hinzu.

IPv6 Rechner addieren: Addiert zur bestehenden Konfiguration einen 
\tIPv6 Rechner und IPv6 Alias hinzu.

Mailserver addieren: Addiert zur bestehenden Konfiguration einen
\tMailserver hinzu. Diese werden alphabetisch angelegt
\ta,b,c,d...

loeschen: Loescht verschiedene Eintraege aus 
\t${SV_DIR}/tinydns/root/data, die vorher ausgewaehlt werden
\tkoennen.

Datenbank neu erstellen: Erstellt eine neue Datenbank fuer tinydns.')
#}}}
### Modeline {{{
### vim:ft=zsh:foldmethod=marker
### vim:set ts=4:                                                                               
### }}}

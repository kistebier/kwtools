# pickdns_help für kwdjbdns{{{
TITLE="${gv_Configuration}::${lv_Pickdns}::${gv_Help}"
MSG=$(gettext 'Ansicht: Zeigt alle Einträge in
\t${SV_DIR}/pickdns/root/data.

Rechner addieren: Addiert zur bestehenden Konfiguration einen 
\tvollqualifizierten Rechnernamen und dessen IP hinzu, der
\tberücksicht werden soll.

Rechner ignorieren: Addiert zur bestehenden Konfiguration einen 
\tvollqualifizierten Rechnernamen und dessen IP hinzu, der
\tignoriert werden soll.

Ort IP-Prefix: Addiert zur bestehenden Konfiguration einen 
\tOrt und einen IP-Prefix. Der Ort muss aus 2 Zeichen bestehen.
\t(Bsp: Ort=de, wie Deutschland, IP_Prefix=192.168.0).

Ort Rechner addieren: Addiert zur bestehenden Konfiguration einen 
\tRechnernamen, IP-Adresse und einen Ort der berücksicht werden
\tsoll. Der Ort muss aus 2 Zeichen bestehen (Bsp: de) und schon
\tvorher bekannt sein.

Ort Rechner ignorieren: Addiert zur bestehenden Konfiguration einen 
\tRechnernamen, IP-Adresse und einen Ort der ignoriert werden
\tsoll. Der Ort muss aus 2 Zeichen bestehen (Bsp: de) und schon
\tvorher bekannt sein.

NOTIZ: Im Anschluss wird die Konfigurationsdatei sortiert.
\t1. nach Ort:IP-Prefix
\t2. nach berucksichtigten Rechner:IP:Ort
\t3. nach ignorierten Rechner:IP:Ort
\t4. nach berucksichtigten Rechner:IP
\t5. nach ignorierten Rechner:IP

löschen: Löscht verschiedene Einträge aus 
\t${SV_DIR}/pickdns/root/data, die vorher ausgewählt werden
\tkönnen.

Datenbank neu erstellen: Erstellt eine neue Datenbank für pickdns.')
#}}}
### Modeline {{{
### vim:ft=zsh:foldmethod=marker
### vim:set ts=4:                                                                               
### }}}

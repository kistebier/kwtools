# server_script_help für kwrsync_backup{{{
TITLE="${lv_Host_Conf}::${gv_Help}"
MSG=$(gettext 'Auswahl: Auswahl eines Script oder Programm das ausgeführt werden
\tsoll.

Eingabe: Eingabe eines Befehls. Dieser kann auch ein Befehl sein
\tder auf entfernten Rechnern ausgeführt werden soll. Bei Befehlen
\tdie auf entfernten Rechnern ausgeführt werden muss eine remote
\tShell angegeben werden.

SYNTAX:
/bin/date "+ backup of localhost started at %c"
ssh root@example.com "mysqldump -A > /var/db/dump/mysql.sql"
')
#}}}
### Modeline {{{
### vim:ft=zsh:foldmethod=marker
### vim:set ts=4:                                                                               
### }}}

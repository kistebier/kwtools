# kwmutt_print_help für kwmutt{{{
TITLE="${lv_Print}::${gv_Help}"
MSG=$(gettext 'print: Bestimmt, ob Mutt vor dem Drucken eine Bestätigung
\tverlangt oder nicht. Diese Variable ist für Leute nuetzlich, die
\toft versehentlich "p" drücken. 
\tDefault ist print="ask-no" (Nein mit Nachfrage)

print_command: Diese Variable enthält den Befehl, an den
\tNachrichten weitergeleitet (gepiped) werden sollen, um sie
\tauszudrucken.
\tDefault ist print_command="lpr"

print_decode: Diese Variable wird in Verbindung mit dem Befehl
\tprint-message benutzt. Falls sie gesetzt ist, wird die Nachricht
\tdekodiert, bevor sie an das externe Programm, das durch
\t$print_command bestimmt wird, weitergeleitet wird. Falls die
\tOption nicht gesetzt ist, findet keine Bearbeitung der Nachricht
\tvor dem Drucken statt. Letzteres kann dann nuetzlich sein, falls
\tSie einen guten Druckerfilter haben, der in der Lage ist, E-Mails
\tordentlich für den Ausdruck zu formatieren.
\tDefault ist print_decode="yes"

print_split: Diese Variable wird in Verbindung mit dem Befehl
\tprint-message benutzt. Falls sie gesetzt ist, wird der Befehl, der
\tdurch $print_command bestimmt wird, einmal für jede zu druckende
\tNachricht aufgerufen. Falls sie ungesetzt ist, wird der Befehl nur
\teinmal aufgerufen, und alle Nachrichten werden zu einer verbunden
\tmit einem Formfeed als Nachrichtentrenner. 
\tDefault ist print_split="no"
')
#}}}
### Modeline {{{
### vim:ft=zsh:foldmethod=marker
### vim:set ts=4:                                                                               
### }}}
